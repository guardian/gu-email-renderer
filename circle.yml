
dependencies:
  pre:
    - curl -o $HOME/google_appengine_1.9.21.zip https://storage.googleapis.com/appengine-sdks/featured/google_appengine_1.9.21.zip
    - unzip -q -d $HOME $HOME/google_appengine_1.9.21.zip

test:
  override:
    - python test_runner.py $HOME/google_appengine